[build-system]
requires = ["maturin>=1.8.3"]
build-backend = "maturin"

[project]
name = "ast-net"
version = "0.1.0"
description = "Antimicrobial suseptibility testing network"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "maturin>=1.10.0",
]
